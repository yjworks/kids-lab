# 키즈랩 전수조사 테스트 목록

한 번 실행(`node tests/run.js`)으로 아래 케이스를 모두 돌리고 `tests/report.md`에 결과표를 남긴다.
기기는 Pixel 7(412×839, 터치)이 기본이고, 표시된 케이스만 PC(1366×768)와 화면 10종을 쓴다.
판정 기준에 적힌 수치를 모두 만족해야 PASS.

## S. 정적 검사 (브라우저 없이)
| ID | 무엇을 | 합격 기준 |
|---|---|---|
| S01 | 앱 파일 구조 | `shared/apps.js`의 앱마다 `apps/<id>/index.html`이 있고, `<body data-app>`가 id와 같고, `</html>`로 끝나며, `<script>` 여는 수 = 닫는 수 |
| S02 | 스크립트 문법 | 모든 HTML 인라인 스크립트와 `shared/*.js`, `apps/**/*.js`, `sw.js`가 문법 오류 없이 해석됨 |
| S03 | 앱 목록 | id·이름·아이콘 중복 0, `cat`이 런처 묶음 목록에 있음, `svg` 아이콘 파일 존재, `how` 설명 있음 |
| S04 | 오프라인 캐시 목록 | `sw.js` 목록의 파일이 모두 존재, 배포 파일(apps·shared·icons) 중 목록에 빠진 것 0 |
| S05 | 미션 | 미션의 앱이 존재하고, 미션 이벤트(`correct`는 퀴즈 엔진, 그 밖은 `event('키')`)를 그 앱이 실제로 냄 |
| S06 | 이모지 호환 | Windows 10에서 네모로 보이는 이모지(Emoji 13.0 이상) 사용 0 |
| S07 | 설치 정보 | manifest의 아이콘 파일이 모두 존재 |
| S08 | 손으로 붙인 조사 | 낱말 변수 뒤에 받침 따라 바뀌는 조사를 직접 붙인 곳 0 (앞말 받침이 고정인 곳은 확인 후 예외 목록) |

## R. 앱별 실행 (43개 앱 전부)
| ID | 무엇을 | 합격 기준 |
|---|---|---|
| R01 | 첫 화면 (폰) | 콘솔 오류 0, 머리말 있음, 메뉴·책장 1개 이상, 가로 넘침 0 |
| R02 | 모든 메뉴 완주 (폰) | 메뉴마다 들어가서, 퀴즈면 첫 문제는 오답 → ✗ 표시·"아니에요" 띠 확인 후 정답, 나머지는 정답으로 한 판 끝까지 → 결과 화면 도달. 오류 0, 막힘 0, 가로 넘침 0 |
| R03 | 첫 화면 (PC) | 콘솔 오류 0, 가로 넘침 0 |

## Q. 문제 생성 (모든 퀴즈 모드 × 단계 1~5 × 150문제)
| ID | 무엇을 | 합격 기준 |
|---|---|---|
| Q01 | 정답 수 | 모든 문제에서 정답 정확히 1개 |
| Q02 | 보기 중복 | 같은 보기 0 |
| Q03 | 빈 값 | 화면·음성 글에 undefined·NaN·null 0 |
| Q04 | 조사 | 받침 뒤 "를·와·예요·로"(ㄹ+로 제외), 받침 없는 뒤 "을·은·으로·이에요" 0 (가을·마을·노을 제외), "(을)를" 같은 괄호 조사 0 |
| Q05 | 뜻 검사 | 이야기 수학: 읽어 주는 식의 계산값 = 정답 / 쌓기나무: 그려진 나무 수 = 정답 / 알뜰 비교: 정답 쪽 한 개 값이 더 쌈 / 영어 문장: "a + 모음" 0 |

## I. 상호작용 흐름
| ID | 무엇을 | 합격 기준 |
|---|---|---|
| I01 | 칠교 7문제 (마우스 끌기) | 7/7 완성 |
| I02 | 칠교 (손가락 끌기) | 2/2 완성, 끄는 동안 페이지 스크롤 0 |
| I03 | 자판 연습 | 조합기 12낱말 모두 일치, 한글 낱말 8개·자리 20개·영어 낱말 8개를 실제 키로 쳐서 결과 화면 |
| I04 | AI 가르치기 | 두 과제 가르치기→시험 6문제→결과, "로보의 실수" 6단계가 ❌ ❌ ⭕ ⭕ 순서 |
| I05 | 저금통 | 목표를 사고 "샀어요" 화면 |
| I06 | 영어 그림책 6권 | 모든 권을 끝까지 넘기고 질문을 정답으로 완주, 책장 6/6, 쪽 넘김 단추가 화면 안 |
| I07 | 영어 문장 만들기 | 6문장 모두 한 번에 완성 |
| I08 | 지구 약속 | 3개 체크 → 새로고침 뒤에도 3개 |
| I09 | 읽기 책 25권 | 모든 권 끝까지 넘김, 쪽 넘침 0, 옛이야기·탈무드 처음부터 열림 |
| I10 | 읽기 지문 21편 | (passage) 오류 0 |
| I11 | 따라 쓰기 151자 | 실제 입력으로 100% 통과 |
| I12 | 낙서 차단 | 칸 전체 칠하기·점 하나·지그재그 0% (기본 글자·받침·한자 모두) |
| I13 | 필사 판정 | 제대로 따라 씀 32/32, 칸 칠하기 0/32, 점 0/32, 글꼴에 없는 글자 0 |
| I14 | 고전 필사 | 한 문장을 끝까지 쓰면 필사 노트에 1개 |
| I15 | 엘리베이터 | 건물 20채 오류 0 |
| I16 | 오락실 | 폰·PC 오류 0 |
| I17 | 계산기 | 오류 0 |

## L. 런처
| ID | 무엇을 | 합격 기준 |
|---|---|---|
| L01 | 아이콘·묶음 | 아이콘 43, 묶음 제목 11, 묶음별 개수 합 43, 탭마다 그 묶음만 |
| L02 | 창·미션·부모 | 창 열기/닫기, 미션 4개, 부모 통계 표시, 오류 0 |
| L03 | 뒤로 가기 | 퀴즈 → 앱 메뉴 → 앱 닫힘 → 안내 → 나감 순서, 설정 창은 뒤로 가기로 닫힘 |
| L04 | 화면 10종 | 모든 화면에서 가로 넘침 0, 잘림 0, 겹침 없음, 오류 없음 |
| L05 | 사용 시간 제한 | 제한 도달 시 잠김, 부모 해제 후 풀림 |
| L06 | 오답 노트 | 틀린 문제가 저장되고, 다시 맞히면 하나 줄어듦 |
